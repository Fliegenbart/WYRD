{
  "name": "create-wyrd",
  "version": "0.1.0",
  "description": "Create WYRD agents — the open coordination layer for the agent internet",
  "type": "module",
  "bin": {
    "create-wyrd": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsup",
    "dev": "tsx src/index.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@wyrd/identity": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.4.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0"
  }
}
