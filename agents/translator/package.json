{
  "name": "@wyrd/agent-translator",
  "version": "0.1.0",
  "private": true,
  "description": "WYRD text translation agent",
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsup src/index.ts --format esm",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@wyrd/sdk": "workspace:*",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "tsup": "^8.4.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0"
  }
}
